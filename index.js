import { getPosts } from './postController.js';
console.log(getPosts());
// const { generateRandomNumber, celciusToFarenhite } = require('./utils');
// console.log(`Random number: ${generateRandomNumber()}`);
// console.log(`Celcius to Farenhite: ${celciusToFarenhite(0)}`);
